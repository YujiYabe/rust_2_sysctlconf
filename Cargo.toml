[package]
name = "sysctl-conf"
version = "0.1.0"
edition = "2024"
description = "A small parser for Linux sysctl.conf-style files"
license = "MIT"

[dependencies]
serde_json = "1"

[lib]
name = "sysctl_conf"
path = "src/lib.rs"
